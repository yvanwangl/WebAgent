import {defineMessages} from 'react-intl';

const messages = defineMessages({
    onOkText: {
        id: 'common.modalAlert.onOkText',
        defaultMessage: '确定'
    }
});

export default messages;